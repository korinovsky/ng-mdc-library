import { DialogFooterDirective } from './footer.directive';

describe('DialogFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogFooterDirective();
    expect(directive).toBeTruthy();
  });
});
