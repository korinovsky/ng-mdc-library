import { DialogTitleDirective } from './title.directive';

describe('DialogTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogTitleDirective();
    expect(directive).toBeTruthy();
  });
});
